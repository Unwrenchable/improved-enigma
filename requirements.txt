# Core dependencies for laser engraving file converter
# Note: The basic version works without external dependencies
# For production use with full conversion capabilities, install these:

# Image processing (for raster conversion)
# Pillow>=10.0.0

# Vector file handling
# svgwrite>=1.4.3
# ezdxf>=1.1.0
# cairosvg>=2.7.0

# For AI/EPS conversion (optional - can use Inkscape CLI instead)
# reportlab>=4.0.0
