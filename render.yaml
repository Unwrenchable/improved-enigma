services:
  - type: web
    name: improved-enigma
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 web_app:app
    envVars:
      - key: FLASK_DEBUG
        value: "false"
